# ==== API管理系统环境配置 ====

# JWT安全配置 (生产环境必须修改！)
JWT_SECRET=your-very-long-and-secure-jwt-secret-key-at-least-32-characters-long-please-change-in-production
JWT_EXPIRES_IN=24h

# 应用配置
PORT=5000
NODE_ENV=development

# 域名配置
DOMAIN_ENABLED=false
# 如果启用域名访问，设置为true并填写完整域名URL
# DOMAIN_URL=https://yourdomain.com

# SSH配置
DEFAULT_SSH_PORT=22
SSH_TIMEOUT=30000

# 日志配置
LOG_LEVEL=info
LOG_FILE=logs/app.log

# 安全配置
MAX_COMMAND_LENGTH=1000
ENABLE_WHITELIST=true

# 数据库配置 (SQLite - 无需额外配置)
# DATABASE_PATH=database.sqlite

# ==== 域名配置说明 ====
# 当 DOMAIN_ENABLED=true 时：
# - 前端会使用 DOMAIN_URL 作为API的baseURL
# - 适用于通过域名（如 https://yourdomain.com）访问的情况
# 
# 当 DOMAIN_ENABLED=false 时：
# - 前端使用相对路径访问API
# - 适用于直接通过IP:端口访问的情况

# ==== 重要安全提醒 ====
# 1. JWT_SECRET 必须是至少32个字符的随机字符串
# 2. 生产环境中不要使用默认值
# 3. 定期更换JWT密钥以提高安全性
# 4. 可以使用以下命令生成安全的随机密钥:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"